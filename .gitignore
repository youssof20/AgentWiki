# Env and secrets — never commit
.env
.env.local
.env.*.local
*.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual envs
.venv/
venv/
ENV/
env/

# Cursor (rules and local config — keep private)
.cursor/

# Game day context (fill in on the day — keep private)
game-day-brief.md

# Local logs context
logs.md

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Local data (if you keep memory in repo by mistake)
memory.json
*.db
*.sqlite

# Logs (optional — remove if you want to track logs.md in git)
# logs.md
